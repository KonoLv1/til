# 気になる単語リスト

## A

* **Architecture（アーキテクチャ）**

設計思想・設計概念のこと。

* **AtCoder**

オンライン競技プログラミング大会サービスのひとつ。

## B

* **Basic Authentication（ベーシック認証）**

Webサイトに対して必要最低限の認証設定をかけること。セキュリティレベルとしては低いものとなっており改竄や盗聴を防ぐ上位版としてDigest認証がある。

## C

** Camel case（キャメルケース）**
単語の頭文字を大文字にすること。命名規則の一つ。

* **Conflict（コンフリクト）**

対立、衝突など。GitHubにおいてはマージできない現象をコンフリクトと呼ぶ。

* **Compile（コンパイル）**

プログラミング言語で書かれたソースをコンピュータ上で実行可能なオブジェクトコードに変換すること。

* **Cross-Site Request Forgeries（クロスサイトリクエストフォージェリ）**

CSRF（シーサーフ）と呼ばれることもある。

攻撃者はブラウザなどのユーザークライアントを騙し意図しないリクエストをWebサーバに送信させる。

* **CSV（Comma Separated Value）**

カンマ区切りの値が入っているファイルのこと。

## D

* **Data mining（データマイニング）**

統計学、パターン認識、人工知能などのデータ解析技法を大量のデータに網羅的に適用することで知識を取り出す技術のことである。

* **Digital Millennium Copyright Act（ディジタルミレニアムコピーライトアクト）**

デジタルコンテンツに関する著作権の基準

* **Domain Driven Design（ドメインドライブンデザイン）**

ソフトウェア設計の手法

* **Digest Authentication(Digest認証)**

HTTPの認証方法の一つ。ユーザ名とパスワードをMD5でハッシュ（ダイジェスト）化して送る。Basic認証では防げなかった盗聴や改竄を防ぐために考案された。

## G

* **GAFA（ガーファ）**

アメリカ合衆国に本拠を置く、Google、Amazon.com、Facebook、Apple Inc. の4つの主要IT企業の頭文字を取って総称する呼称

## H

* **Human Interface Guidelines（ヒューマンインターフェースガイドライン）**

すべてのグラフィカル・ユーザー・インターフェイス要素が見た目が一貫していて、すべてのユーザーにとって本能的にダイアログを使用できるようにするためのガイドライン。

## I

## J

## K

## L

* ** Linux Kernel（リナックスカーネル）**

メモリ管理、ファイルシステム、プロセス管理、デバイス制御など、ＯＳとしてコンピュータやアプリケーションソフトを制御するための最も基本的機能だけを実装したソフトウェアです。
カーネルだけでアプリケーションを動作させることはできないため、ＯＳとして機能させるためにはglibcなどのライブラリなどを組み合わせる必要がある。

## M

* **Magic Number（マジックナンバー）**

プログラムコード中の何を意図するのか分からない数値
可読性を下げバグの要因となる。

* **MVC（Model View Controller）**

アプリケーションの内部データを、ユーザが直接参照・編集する情報から隔離する設計のこと。

* **MVVM（Model-View-ViewModel)**

* **Multiple Instruction Single Data（マルチプルインストラクションシングルデータ）**

複数の命令で単一のデータを取り扱う方式の並列処理を実現するプロセッサのアーキテクチャ
理論上は存在するが実装例はない。

* **Multiple Instruction Multiple Data（マルチプルインストラクションマルチプルデータ）**

複数の命令で複数のデータを扱う方式の並列処理を実現するプロセッサのアーキテクチャ

## N





## O

* **OpenFlow**

ネットワーク上でスイッチまたはルーターのフォワーディング・プレーンへのアクセスを可能にする通信プロトコル。

## P

* **Portable Document Format（ポータブル・ドキュメント・フォーマット）**

アドビシステム社が開発提唱する電子上の文書に関するファイルフォーマット。拡張子は.pdf

* **Penetration Test（ペネトレーションテスト）**

ネットワーク接続されているコンピュータシステムに対して実際に既知の技術を用いて侵入を試みてシステムに脆弱性がないか判断するテスト。

## Q

## R

* **Redundant Arrays of Independent Disks（レイド）**

安価な複数台の磁気ディスクを組み合わせて全体として高い信頼性を得る方式

**RAID 0**

複数のディスクにデータを分散して書きこむストライピング

**RAID 1**

2台の磁気ディスクに同じ内容を書き込むミラーリング

**RAID 2**

ストライピング＋エラー訂正用のハミング符号をディスクに書き込む。最少でも5台構成となる。

**RAID 3**

ストライピング＋エラー修正用にパリティビットを1台のディスクに書き込む。

**RAID 4**

RAID3のパリティビットの書き込みをブロック単位で行う。

**RAID 5** 

パリティビットもデータと同様に分散して書き込む。

## S

* **Software Defined Networking（ソフトウェアデファインドネットワーキング）**

ソフトウェアにより柔軟に制御できるネットワーキングのこと。

* **Sales Force Automation（セールスフォースオウトメーション）**

営業支援システムのこと。

* **Single Instruction Single Data（シングルインストラクションシングルデータ）**

単一の命令で単一のデータを処理する方式の並列処理を実現するプロセッサのアーキテクチャ

* **Single Instruction Multiple Data（シングルインストラクションマルチプルデータ）**

単一の命令で複数のデータを処理する方式の並列処理を実現するプロセッサのアーキテクチャ

## T

## U

## V

* **VIPER（View-Interactor-Presenter-Entity-Router）**

単一責任の原則のもと適切に分割するような設計

## W

* **WORA（Write once run anywhere）**

一度書いたものならどこでも動くこと

## X

## Y

## Z